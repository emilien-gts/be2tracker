<div class="fixed top-0 left-0 h-screen w-64 flex flex-col justify-between border-e bg-white">
    <div class="px-4 py-6">
        <span class="grid h-10 w-32 place-content-center rounded-lg text-pink-500 font-semibold">
            Be2tracker
        </span>

        <ul class="mt-6 space-y-1">
            <li>
                <a
                    href="{{ path('app_bankroll_bankroll_index') }}"
                    class="block rounded-lg px-4 py-2 text-sm font-medium text-pink-500
                    {{ 'app_bankroll_' in app.current_route ? 'bg-pink-100"' }}"
                >
                    Bankrolls
                </a>
            </li>
            <li>
                <a
                    href="{{ path('app_betcombination_index') }}"
                    class="block rounded-lg px-4 py-2 text-sm font-medium text-pink-500
                    {{ 'app_betcombination_' in app.current_route ? 'bg-pink-100"' }}"
                >
                    Bet combination
                </a>
            </li>
        </ul>
    </div>
</div>
